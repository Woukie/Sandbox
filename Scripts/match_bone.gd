extends RigidBody3D

@export var TARGET_OFFSET := Vector3.ZERO # Local offset which bone aligns to
@export var BONE := Skeleton3D # Target bone

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
